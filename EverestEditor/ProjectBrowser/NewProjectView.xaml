<UserControl x:Class="EverestEditor.ProjectBrowser.NewProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EverestEditor.ProjectBrowser"
             mc:Ignorable="d" Width="550" Height="450"
             d:DesignHeight="450" d:DesignWidth="550">
    <UserControl.DataContext>
        <local:NewProject />
    </UserControl.DataContext>
    <StackPanel Background="LightBlue" x:Name="chooseTemplateStack">
        <StackPanel Orientation="Horizontal" Margin="0 20 0 0" HorizontalAlignment="Center">
            <ListBox Width="150" Height="250" ItemsSource="{Binding Templates}" x:Name="templatesListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Icon}" Width="15" Height="15" Margin="0 0 5 0" />
                            <TextBlock Text="{Binding ProjectType}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Border Width="350" Height="250">
                <Border.Background>
                    <ImageBrush ImageSource="{Binding Path=SelectedItem.Splash , ElementName=templatesListBox}" />
                </Border.Background>
            </Border>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 30 0 0">
            <TextBlock Text="Name" TextAlignment="Center" Height="20" Width="50" HorizontalAlignment="Center" />
            <TextBox Width="450" Height="20" Text="{Binding Path=SelectedItem.TemplateName , ElementName=templatesListBox}" x:Name="templateName" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
            <TextBlock Text="Path" TextAlignment="Center" Height="20" Width="50" HorizontalAlignment="Center" />
            <TextBox Width="380" Height="20" Text="{Binding ProjectPath}" />
            <Button Content="Browse" Width="65" Height="20" Margin="5 0 0 0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 30 0 0">
            <Button Content="Create" Width="100" IsDefault="True" />
            <Button Content="Exit" Width="100" Margin="30 0 0 0" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</UserControl>